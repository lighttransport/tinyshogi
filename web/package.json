{
  "name": "tinyshogi-web",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "make EM_CACHE=${EM_CACHE:-/tmp/tinyshogi-emcache}",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
